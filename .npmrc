# Use legacy peer dependency resolution for the whole project.
# React 19 + shadcn/ui (Radix) often have peer ranges that npm 7+ rejects;
# this makes `--legacy-peer-deps` the default for every install, including
# the ones shadcn's CLI triggers automatically.
legacy-peer-deps=true
